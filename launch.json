{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Xdebug com Laravel Sail",
            "type": "php",
            "request": "launch",
            "hostname": "0.0.0.0",
            "port": 9003,
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}"
            }
        }
    ]
}